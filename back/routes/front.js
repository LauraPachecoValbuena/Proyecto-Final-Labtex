var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function(req, res, next) {
  res.render('')  //aqui tengo que poner la rfuta de la 1º pagina.
});

module.exports = router;

